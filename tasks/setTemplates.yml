---
- name: Set Postfix configuration file templates
  when: item.value.enabled
  template:
    src: "{{ item.value.templatefile }}"
    dest: "{{ item.value.templatepath }}"
  with_dict: "{{ aspects_postfix_templates }}"
  notify: reload postfix
  tags:
  - aspects_postfix
